1)
SELECT COUNT(`id`), YEAR(`enrolment_date`) 
FROM `students` 
GROUP BY YEAR(`enrolment_date`);

2)
SELECT COUNT(`id`), `office_address`
FROM `teachers` 
GROUP BY `office_address`;

3)
SELECT COUNT(`exam_id`), `exam_id`, ROUND(AVG(`vote`), 2) 
FROM `exam_student` 
GROUP BY `exam_id`;